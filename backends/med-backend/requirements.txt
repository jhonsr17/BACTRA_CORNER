# --- Core API ---
fastapi==0.115.0
uvicorn==0.30.6

# --- Supabase client ---
supabase==2.6.0

# --- HTTP requests (para comunicar con n8n y otros servicios) ---
requests==2.32.3

# --- Environment variables ---
python-dotenv==1.0.1

# --- Data validation (opcional, mejora modelos de entrada/salida) ---
pydantic==2.9.2

# --- CORS (si usas frontend separado) ---
starlette==0.38.2

# --- Debugging / utilidad ---
httpx==0.27.0
